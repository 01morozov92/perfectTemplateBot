databaseChangeLog:
  - changeSet:
      id: users
      author: Morozov
      changes:
        - createTable:
            tableName: users
            columns:
              - column:
                  name: id
                  type: bigint
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: name
                  type: varchar(200)
                  constraints:
                    nullable: true
              - column:
                  name: amount_of_days
                  type: int
                  constraints:
                    primaryKey: true
                    nullable: false